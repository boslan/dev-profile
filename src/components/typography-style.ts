import { css, CSSResult } from 'lit-element';

// language=CSS
export const typographyStyle: CSSResult = css`
    label {
        color: var(--secondary);
        font-size: 16px;
        margin-right: 5px;
    }

    .control {
        display: flex;
        flex-wrap: wrap;
    }
`;
